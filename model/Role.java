package model;

/**
 * Role Enum - Defines user roles in the system
 * Used to specify the type of user (Student, Evaluator, or Staff/Coordinator)
 */
public enum Role {
    STUDENT,
    EVALUATOR,
    STAFF
}
